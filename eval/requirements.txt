httpx>=0.27.0
rich>=13.7.0
anthropic>=0.40.0
pydantic>=2.0.0
# Used by probes/agent_prototype.py for headless screenshot capture of the
# running prototype. Must install browser binary separately:
#   .venv/bin/playwright install chromium
playwright>=1.50.0
