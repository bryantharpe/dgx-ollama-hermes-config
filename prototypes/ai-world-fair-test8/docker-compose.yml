services:
  app:
    build: .
    image: ${PROTOTYPE_NAME:-prototype}:latest
    container_name: ${PROTOTYPE_NAME:-prototype}
    restart: unless-stopped
    ports:
      - "${PROTOTYPE_PORT:-9100}:8000"
    volumes:
      - ./data:/app/data
    environment:
      - PYTHONUNBUFFERED=1
